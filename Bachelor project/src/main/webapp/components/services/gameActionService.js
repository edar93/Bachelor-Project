'use strict';

var gameActionService = function(backendGateway){
    this.faceCard = faceCard;

    function faceCard(){

    }
};

angular.module('portRoyalApp.gameActionService', [])
    .service('gameActionService', gameActionService);